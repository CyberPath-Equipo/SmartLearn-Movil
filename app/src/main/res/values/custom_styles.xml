<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ========== ESTILOS DE BOTONES ========== -->
    <style name="Button.Primary" parent="Widget.Material3.Button">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">?attr/colorOnBackground</item>
        <item name="cornerRadius">12dp</item>
        <item name="android:elevation">8dp</item>
        <item name="android:stateListAnimator">@animator/button_animation</item>
    </style>

    <style name="Button.Secondary" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">12dp</item>
        <item name="strokeWidth">2dp</item>
    </style>

    <!-- ========== ESTILOS DE CARDS ========== -->
    <style name="CardView.Modern" parent="Widget.Material3.CardView.Outlined">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">8dp</item>
        <item name="strokeWidth">0dp</item>
    </style>

    <!-- ========== ESTILOS DE TEXT INPUTS ========== -->
    <style name="TextInputStyle" parent="Widget.Material3.TextInputEditText.OutlinedBox">
        <item name="android:textSize">16sp</item>
        <item name="android:padding">16dp</item>
    </style>

    <!-- ========== ESTILOS DE BARRAS DE BÚSQUEDA ========== -->
    <style name="SearchViewStyle">
        <item name="android:textColorHint">@color/colorNeutral400</item>
        <item name="android:textColor">@color/colorOnSurface</item>
    </style>

    <!-- ========== ANIMADOR DE ELEVACIÓN PARA BOTONES ========== -->
    <style name="FloatingActionButtonStyle" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="android:elevation">8dp</item>
        <item name="android:stateListAnimator">@animator/button_animation</item>
    </style>

</resources>

